fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.0

sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

pandas==2.2.3
openpyxl==3.1.5
yfinance>=1.5.1

pyjwt[crypto]==2.10.1
httpx==0.28.1

# LLM analyst + billing (phase 2). Groq needs no SDK — it speaks the OpenAI wire shape
# over the httpx we already have.
anthropic==0.69.0
stripe==11.4.1

# Hierarchical agent briefing (phase 3). LangGraph orchestrates the orchestrator ->
# market -> sector graph; its nodes are plain functions that call our own AnalystModel
# seam, so no langchain model packages are needed. Pins hold pydantic at 2.10.4.
langgraph==1.2.9

pytest==8.3.4
